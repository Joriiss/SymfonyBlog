{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h2 class="p-3 text-center">Add a student</h2>
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}